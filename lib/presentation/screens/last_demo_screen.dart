import 'package:flutter/material.dart';

class LastDemoScreen extends StatelessWidget {
  const LastDemoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Moved to Sidebar  >  App Settings"),
      ),
    );
  }
}
